{
  "name": "gstack-skill-gallery",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run generate-data && vite",
    "build": "npm run generate-data && vite build",
    "preview": "vite preview",
    "generate-data": "tsx scripts/parse-skills.ts"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vite": "^6.0.0",
    "yaml": "^2.3.0"
  }
}
